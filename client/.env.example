# URL do backend (Socket.io / sinalização WebRTC).
# Vazio em desenvolvimento local = mesma origem (via proxy do Vite).
# Em produção (ex: http.server.onrender.com) define aqui, sem barra final.
VITE_SOCKET_URL=
